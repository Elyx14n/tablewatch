INSERT INTO
  player_anomalies
SELECT
  player_id,
  'WONGING_ENTRY_ANOMALY' AS anomaly_type,
  LEAST(true_count / 4.0, 1.0) AS anomaly_confidence,
  event_time AS anomaly_detected_at,
  true_count AS wonging_score
FROM
  seat_events
WHERE
  true_count >= 1.0
